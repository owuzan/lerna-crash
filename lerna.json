{
  "$schema": "node_modules/lerna/schemas/lerna-schema.json",
  "version": "0.2.11",
  "npmClient": "yarn",
  "packages": [
    "packages/*"
  ],
  "command": {
    "publish": {
      "version": "yarn preversion && lerna publish --contents dist"
    }
  }
}
